<li class="text-left">
	{{!-- <button class="delete" data-id="{{id}}">DELETE</button> --}}
	{{#if devoured}}
	{{else}}<button class="devoured" data-id="{{id}}" style="font-size: 0.5rem; background: #ffa369; color: #fff;">Update</button>
	{{/if}}
	{{burger_name}}
</li>